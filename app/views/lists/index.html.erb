<%= button_to "Log Out", destroy_user_session_path, method: :delete %>
<h2>Welcome <%= current_user.first_name %> !</h2>
<hr />
<%= link_to "Add Bucket List", new_list_path %>
<br />
<h3><%= @lists.length == 0 ? "No Lists" : nil %></h3>
<ul>
  <% @lists.each do |list| %>
    <li>
      <%= list.name %>
    </li>
  <% end %>
</ul>
